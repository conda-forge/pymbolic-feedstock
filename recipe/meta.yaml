{% set name = "pymbolic" %}
{% set version = "2024.1" %}
{% set sha256 = "c698a4541d7be23ac289d41e300c41056b7b3e879266a898c2e8290e6e96675f" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.6
    - setuptools
    - pytools
    - pip
  run:
    - python >=3.6
    - numpy
    - pytools

test:
  requires:
    - pytest
    - matchpy
  imports:
    - {{ name }}
  source_files:
    - test
  commands:
    - (cd test && python -m pytest)        # [unix]
    - cd test && python -m pytest      # [win]

about:
  home: http://pypi.python.org/pypi/pymbolic
  dev_url: http://github.com/inducer/pymbolic
  doc_url: https://documen.tician.de/pymbolic
  license: MIT
  license_file: LICENSE
  summary: A package for symbolic computation

extra:
  recipe-maintainers:
    - inducer
